<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <application
        android:name=".SpeechApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SeSAC_Speech"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <activity
            android:name=".ui.splash.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.SeSAC_Speech.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.login.LoginActivity"
            android:exported="false" />

        <activity
            android:name=".ui.signup.SignUpActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- D-6: 가입 다단계 Step2 (성별·생년월일·취미·태그) -->
        <activity
            android:name=".ui.signup.SignUpProfileStepActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- D-6: 가입 설문 (스킵 불가 — 뒤로가기 경고 다이얼로그) -->
        <activity
            android:name=".ui.survey.SurveyActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustPan" />

        <activity
            android:name=".ui.chat.ChatActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".ui.profile.SettingActivity"
            android:exported="false" />

        <activity
            android:name=".ui.profile.ProfileEditActivity"
            android:exported="false" />

        <activity
            android:name=".ui.record.RecordingTestActivity"
            android:exported="false" />

        <!-- P3-26: 세션 플로우 (로딩 → 문제풀이 → 이야기 → 결과) -->
        <activity
            android:name=".ui.learning.LearningSessionLoadingActivity"
            android:exported="false" />

        <activity
            android:name=".ui.learning.ProblemActivity"
            android:exported="false" />

        <activity
            android:name=".ui.learning.StorytellingActivity"
            android:exported="false" />

        <activity
            android:name=".ui.learning.SessionReportActivity"
            android:exported="false" />

        <activity
            android:name=".ui.onboarding.PermissionOnboardingActivity"
            android:exported="false" />

    </application>

</manifest>
