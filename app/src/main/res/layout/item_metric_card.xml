<?xml version="1.0" encoding="utf-8"?>
<!-- D-7 3.3 지표 카드(점수·피드백) — 클릭 확장 시 turns(문제 기록) 표시 -->
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="8dp"
    app:cardBackgroundColor="@android:color/white"
    app:cardCornerRadius="16dp"
    app:cardElevation="2dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center_vertical"
            android:orientation="horizontal">

            <TextView
                android:id="@+id/tvMetricType"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:textAppearance="@style/TextAppearance.SeSAC.H4"
                android:textColor="@color/text_primary"
                tools:text="알아듣기" />

            <TextView
                android:id="@+id/tvMetricScore"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textAppearance="@style/TextAppearance.SeSAC.H3"
                android:textColor="@color/primary"
                android:textStyle="bold"
                tools:text="100.0점" />
        </LinearLayout>

        <TextView
            android:id="@+id/tvMetricFeedback"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="6dp"
            android:textAppearance="@style/TextAppearance.SeSAC.Body"
            android:textColor="@color/text_secondary"
            tools:text="알아듣기 문제를 대부분 정확히 골랐어요." />

        <!-- 확장 영역: 문제 기록 (turns) — 카드 클릭 시 토글 -->
        <LinearLayout
            android:id="@+id/containerTurns"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:orientation="vertical"
            android:visibility="gone" />
    </LinearLayout>
</com.google.android.material.card.MaterialCardView>