<?xml version="1.0" encoding="utf-8"?>
<!-- D-8-C1: gradient-brand radius 22dp (시안 Btn rounded-2xl) — bg_grad_brand의 16dp radius 변형 (키 추가만) -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <gradient
        android:angle="315"
        android:startColor="@color/brand_grad_brand_start"
        android:endColor="@color/brand_grad_brand_end"
        android:type="linear" />
    <corners android:radius="@dimen/radius_xl" />
</shape>
